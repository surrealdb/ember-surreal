# -----------------------------------

general:
  branches:
    only:
      - master

# -----------------------------------

machine:
  timezone:
    UTC
  node:
    version: 6.1.0

# -----------------------------------

dependencies:
  override:
    - make setup
    - make install

# -----------------------------------

test:
  override:
    - make tests
